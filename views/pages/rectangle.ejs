<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title><%= rows[0].name %>'s rectangle</title>
    <link href="style.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <center>
      <h1><%= rows[0].name %>'s rectangle</h1>
	  <div style="width:<%= rows[0].width %>px; height:<%= rows[0].height %>px; background-color:<%= rows[0].color %>; margin:10px;"></div>
      <table>
        <tr>
          <th>Name</th>
          <td><%= rows[0].name %></td>
        </tr>
        <tr>
          <th>Width</th>
          <td><%= rows[0].width %> px</td>
        </tr>
        <tr>
          <th>Height</th>
          <td><%= rows[0].height %> px</td>
        </tr>
        <tr>
          <th>Color</th>
          <td><%= rows[0].color %></td>
        </tr>
      </table>
	  <div style="display:flex; justify-content:center;">
		<a href="/"><button style="margin:4px;">Back</button></a>
		<form action="/delete" method="post" style="margin:4px;">
			<input type="hidden" name="id" value="<%= rows[0].id %>"></input>
			<button type="submit">Delete</button>
		</form>
	  </div>
    </center>
  </body>
</html>